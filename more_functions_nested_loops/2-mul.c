#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers.
 *@a: first character to be multipiled
 *@b: second character to be multipled
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
